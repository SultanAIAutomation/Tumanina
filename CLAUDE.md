# Tumanina (طمأنينة) — تعليمات المشروع لـ Claude Code

تطبيق ويب عربي للأذكار (PWA)، منشور على GitHub Pages:
sultanaiautomation.github.io/Tumanina
المستودع: github.com/SultanAIAutomation/Tumanina (عام)

## البنية الحالية
- `index.html` — الهيكل فقط (HTML)
- `styles.css` — كل الأنماط
- `app.js` — كل منطق التطبيق (بما فيه الضبط اليومي وstreak)
- `data/azkar.js` — بيانات الأذكار لكل الأقسام
- `manifest.json`, `sw.js`, `icons/` — إعداد PWA وoffline caching. `sw.js`: network-first لـ `index.html` وdata/azkar.js فقط، وcache-first لبقية الملفات
- `fonts/thmanyah/` — خطوط Thmanyah Sans (للواجهة) وThmanyah Serif Text (للأذكار)، مستضافة محليًا بإذن من Thmanyah (راجع `fonts/PERMISSION.md`)
- خط Thmanyah Serif Text يُستخدم لكل النصوص بما فيها الآيات القرآنية — قرار مقصود من المستخدم، لا تقترح تغييره مجددًا (لا تغيّر `.quran-text` ولا `.zikr-text` في `styles.css`)
- خط UthmanicHafs1Ver18.woff2 ما زال محمَّلًا كخط احتياطي فقط ولا يُستخدم فعليًا للعرض
- `azkar-review.txt` — ملف تدقيق مؤقت، مُدرَج في `.gitignore` ولا يُرفع

## الأقسام الحالية وأعدادها
أذكار الصباح 30 · المساء 31 · بعد الصلاة 15 · كنوز الذكر 13 · أذكار النوم 10 · أذكار الاستيقاظ 4

## قواعد صارمة يجب اتباعها دائمًا

### فقرة الفضل (الصندوق الأخضر)
تُضاف **فقط** إذا وردت في حديث **صحيح مرفوع** ينص صراحة على فضل/ثواب قول الذكر نفسه.
- الأحاديث الموقوفة أو الضعيفة لا تُضاف كفضل
- شرح معنى الذكر ليس فضلًا — يُترك الصندوق فارغًا في هذه الحالة
- المستخدم يراجع بنفسه كل فقرة فضل مقترحة قبل إدراجها — لا تُدرج فضلًا دون مراجعته
- تدقيق شامل لكل الأذكار تم (سبتمبر 2026): الوحيدة اللي استحقت إضافة فضل هي e1 وp10. الباقي إما بلا حديث فضل منفصل أصلًا، أو حديث ضعيف (رُفض صراحة: e9, e10, e11, e23, m25, m26, m22, m10, p12) — لا تعيد فتح هذه القائمة إلا إذا طلب المستخدم صراحةً
- إذا كان الفضل يشترط قراءة عدة أذكار مجتمعة (مثل السور الثلاث)، وضّح ذلك صراحةً في نص الفضل

### صندوق الملاحظة الرمادي (note)
للهيئة الجسدية (نفث، مسح، وضع اليد تحت الخد) ومناسبة الاستخدام — لا تُخلط مع صندوق الفضل الأخضر

### النص القرآني
- موحّد على الرسم العثماني (Uthmani) من quran.com
- الاستعاذة والبسملة تبقى بالرسم الإملائي في كل موضع (قرار متعمد من المستخدم)
- علامات نهاية الآية بصيغة ﴿n﴾
- الخط: Thmanyah Serif Text لكل النصوص بما فيها الآيات (انظر أعلاه)

### طريقة التحقق قبل أي دفع (push)
المستخدم يصدّر الأذكار عبر أمر node مباشر ويقارنها بمصحف حقيقي بنفسه قبل الموافقة على الدفع عند تغيير محتوى الأذكار.
**اطلب إذنًا صريحًا قبل أي `git push`.**

## بنود مفتوحة (لم تبدأ بعد)
1. إشعارات (الأصعب تقنيًا — غير موثوقة على iOS)

## ملاحظات عامة
- المشروع منفصل تمامًا عن مشاريع المستخدم الأخرى (al-amri-english, Murshid) — لا تُخلط المحتوى أو القرارات بينها
- كان العمل سابقًا عبر Codex؛ هذا الملف يهيئ نفس السياق لجلسات Claude Code
- الضبط اليومي التلقائي وعداد الأيام المتتالية (streak) منفَّذان في `app.js` — منجَزان، ليسا بندًا مفتوحًا
- theme-color يتبع `prefers-color-scheme` تلقائيًا — منجَز
